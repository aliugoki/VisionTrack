# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
alembic==1.13.3
asyncpg==0.30.0
psycopg2-binary==2.9.10

# Validation & settings
pydantic==2.9.2
pydantic-settings==2.6.0
email-validator==2.2.0

# Auth & security
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# Cache & queue
redis==5.1.1
celery==5.4.0

# Realtime
python-socketio==5.11.4

# Object storage
minio==7.2.9

# HTTP client
httpx==0.27.2

# ONVIF camera discovery (Step 2 — Cameras module)
WSDiscovery==2.1.2
onvif-zeep-async==4.1.1

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0
structlog==24.4.0

# Dev tooling
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.7.1
pymupdf==1.24.10

# pgvector — Python binding for the Postgres vector type.
# Required by app.modules.persons.models for ReID embeddings.
pgvector==0.3.6
# pymilvus needs marshmallow<4 because some transitive dep (environs)
# uses marshmallow.__version_info__ which was removed in marshmallow 4.
marshmallow<4
# pymilvus — Python binding for Milvus vector DB (P2.3).
pymilvus==2.4.7
