# Pinned to the same versions the existing ai-worker uses, to keep
# logging/config behavior identical across services.
httpx==0.27.2
redis==5.1.1
pydantic==2.9.2
pydantic-settings==2.6.0
structlog==24.4.0
